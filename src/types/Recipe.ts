interface Recipe {
  title: string;
  ingredients: string[];
  link: string;
  gif: string;
}

export interface PuppyRecipe {
  title: string;
  ingredients: string;
  href: string;
}

export default Recipe;
